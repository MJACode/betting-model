# NFL Monday 2026-09-21 — empty Today / Signals

Game in `games`: `NFL_2026_02_NYG_LA`, `game_date` 2026-09-21, NYG @ LA,
`commence_time` 2026-09-22T00:15:00Z. Scores were null at 22:10 UTC
(kickoff 8:15 PM ET). `picks` had 0 rows for that `game_id` at the same time.

Two separate facts. This change touches only the second.

## 1. Zero BET inserts — floors not moved

Night Watch: nothing cleared the EV floor, so the day wrote 0 BET rows.

Worker, deployment `acd24980`, 21:25–21:30 UTC (5:25 PM ET):

- Prop tick: next kickoff in 2.8h. Odds fetch 5,831 rows, 17 events.
- `nfl_prop_market` card: "No qualifying edges" (sharp pinnacle / betonlineag, min edge 5%).
- Distributional scorer for 2026-09-21: `0 BETs / 93 picks`. Of those, `nfl_prop_tackles_assists` (the one distributional model with `paused = false` in `model_action_thresholds`) was `0 BETs / 8 non-BET`.
- Wind, 21:20 UTC: "No qualifying bets at threshold 11.0 mph."
- Opener: "No qualifying opener bets at |dev| >= 2.0 (17 game(s) watched, 16 inside T-7..T-2)."

`config.MODEL_OWN_EV_FLOOR` still has `nfl_prop_market` 0.20, `nfl_wind_totals` 0.05, `nfl_opener_spread` 0.01. Models with no entry use `min_ev_for` → `GLOBAL_MIN_EV` (0.20 in `config.py`). No floor, cut, or pause was changed. Moving one is a Michael-gate.

The clear fix does not create a BET. Signals stays empty until a row clears its cut and its floor. Today can show an unpaused NONE (`nfl_prop_tackles_assists`). The other eleven distributional models are `paused = true` in `model_action_thresholds`, so the app hides them.

## 2. The clear that emptied the NONE rows

`picks_log`: 1,697 NFL NONE inserts, then 1,697 deletes. The evening refresh at 22:06:56 UTC logged `Cleared unsettled picks for games not yet started`. That pass runs `run_scorer` and does not run `nfl-prop-scoring`.

`run_scorer`'s housekeeping DELETE removes unsettled non-BET rows on games that have not kicked. `config.MODELS` has no NFL game model, so the loop does not re-insert what it deletes. An NFL game also used to fall through to the NHL feature builder (that function always returns a dict), take the per-game DELETE, and land in `rescored`, which then hid the game from the sweep.

Fix: both live clears skip `model_id NOT LIKE 'nfl_prop_%'` (the `nfl_prop_` prefix, including `nfl_prop_market`). An NFL game no longer enters the loop, so the sweep is the clear that sees its rows. Wind and opener are not in the exclusion. BET rows were already spared (`signal_type != 'BET'`).
