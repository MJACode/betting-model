# NFL Monday 2026-09-21 — empty Today / Signals

Game in `games`: `NFL_2026_02_NYG_LA`, `game_date` 2026-09-21, NYG @ LA,
`commence_time` 2026-09-22T00:15:00Z. Scores were null at 22:10 UTC
(kickoff 8:15 PM ET).

Two separate facts. This change touches only the second.

## 1. Zero BET inserts — cuts and pauses, floors not moved

`picks_log` for `sport = 'NFL'` and `game_date = '2026-09-21'` has no BET row, insert or delete. The 21:26 UTC wave is 93 NONE inserts, one game, `NFL_2026_02_NYG_LA`.

Worker, deployment `acd24980`, 21:25–21:30 UTC (5:25 PM ET):

- Prop tick: next kickoff in 2.8h. Odds fetch 5,831 rows, 17 events.
- `nfl_prop_market` card: "No qualifying edges" (sharp pinnacle / betonlineag, min edge 5%). No `picks_log` row for that model.
- Distributional scorer for 2026-09-21: `0 BETs / 93 picks`.
- Wind, 21:20 UTC: "No qualifying bets at threshold 11.0 mph." No row.
- Opener: "No qualifying opener bets at |dev| >= 2.0 (17 game(s) watched, 16 inside T-7..T-2)." No row.

`nfl_prop_tackles_assists` is the only unpaused distributional model (`model_action_thresholds`: `paused = false`, `min_prob` 0.70, `min_edge` 0.15). Eight rows in that wave. Max `model_probability` 0.6584. Max `decision_edge` 0.0839. Zero rows cleared both cuts, so the EV floor was never applied.

Two paused models cleared their own cuts and their 0.20 floors. The pause stored them as NONE:

- `nfl_prop_receptions`, `Colby Parkinson Under 3.5 Rec`: probability 0.8064, `decision_edge` 0.1823, `decision_odds` -166. EV = 0.8064 * (1 + 100/166) - 1 = 0.292. Own floor 0.20 (`MODEL_OWN_EV_FLOOR`).
- `nfl_prop_rush_rec_yards`, `Devin Singletary Over 25.5 Ru+Re Yds`: probability 0.6928, `decision_edge` 0.1537, `decision_odds` -117. EV = 0.6928 * (1 + 100/117) - 1 = 0.285. Own floor 0.20.

`config.MODEL_OWN_EV_FLOOR` still has `nfl_prop_market` 0.20, `nfl_wind_totals` 0.05, `nfl_opener_spread` 0.01. Models with no entry use `min_ev_for` → `GLOBAL_MIN_EV` (0.20 in `config.py`). No floor, cut, or pause was changed. Moving one is a Michael-gate.

The clear fix does not create a BET. Signals stays empty until a row clears its cut and its floor on a model that is not paused. Today can show an unpaused NONE (`nfl_prop_tackles_assists`) once the clear stops deleting it. The other eleven distributional models are `paused = true` in `model_action_thresholds`, so the app hides them.

## 2. The rows landed, then the per-game clear removed them

Not a failed insert. The prop scorer committed, and `picks_log` recorded the rows.

Day total, queried 2026-09-21 ~22:30 UTC: 1,697 NFL NONE inserts and 1,697 NFL NONE deletes. Two waves that match the empty `picks` table:

- 93 NONE inserts, 21:26:12.212294–21:26:57.687922 UTC (the `0 BETs / 93 picks` log). One statement deleted all 93 at 22:06:20.334297, one game, NFL only. UFC inserts in the same refresh continue at 22:06:20.888251, MLB through 22:07:52. That timestamp is inside the game loop, so it is the per-game `DELETE … WHERE game_id = %s`, not the sweep that logs `Cleared unsettled picks for games not yet started` after the loop.
- The 6:25 PM ET prop tick inserted 95 NONE rows, 22:26:13.966702–22:26:54.826044. The 6:20 PM ET refresh (`hour=18-23`, `minute=*/10`) was still in its loop and deleted all 95 at 22:27:24.959056, one game. UFC scoring in that same pass resumes at 22:27:25.460568.

`config.MODELS` has no NFL game model, so the loop does not re-insert what it deletes. An NFL game fell through to the NHL feature builder (that function returns a dict), took the per-game DELETE, and landed in `rescored`, which then hid the game from the sweep. After the 22:27:24 delete, `picks` for `NFL_2026_02_NYG_LA` was 0 again.

Fix: both live clears skip `model_id NOT LIKE 'nfl_prop_%'` (the `nfl_prop_` prefix, including `nfl_prop_market`). An NFL game no longer enters the loop. Wind and opener are not in the exclusion. BET rows were already spared (`signal_type != 'BET'`). Production is still master until this lands and the worker redeploys, so the next evening refresh deletes the next prop tick the same way.
